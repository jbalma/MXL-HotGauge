# HotGauge 
The scripts in this directory are wrappers for 3D-ICE functions that feed the ICE sim
HotGauge's unique inputs (power trace files) and run the simulation. Among other things, 
these scripts build and fill stack and flp templates, check that 3D-ICE output is
in the correct format, and create plots to visualize 3D-ice outputs.
